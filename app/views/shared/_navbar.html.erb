<div class="navbar">
  <!-- Logo -->
  <div class="navbar-logo">
    <%= link_to 'My Holidays Book', root_path %>
  </div>

  <!-- User connected -->
  <% if user_signed_in? %>

  <!-- LARGE DEVICE -->

  <!-- <div class="navbar-right hidden-xs hidden-sm"> -->
  <div class="navbar-right">
      <div class="navbar-item">
        <div class="dropdown">
          <%#if current_user.photo.file.nil? %>
            <% user_avatar = 'user' %>
          <%#else %>
            <%#user_avatar = current_user.photo %>
          <%# end %>
          <%#cl_image_tag user_avatar, class: "avatar dropdown-toggle", id: "navbar-menu", "data-toggle" => "dropdown" %>
          <%=image_tag user_avatar, class: "avatar dropdown-toggle", id: "navbar-menu", "data-toggle" => "dropdown" %>


          <ul class="dropdown-menu dropdown-menu-right navbar-dropdown-menu">
            <!-- <li><%# link_to "Mes voyages", user_path(current_user, page: "resa"), id:"resa-link" %></li> -->
            <li> <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="fas fa-sign-out-alt"></i>  <%= t(".sign_out", default: "Log out") %>
                <% end %>
            </li>
          </ul>

        </div>
      </div>
  </div>

      <%else %>
          <!-- Login link (when logged out) -->
        <div>
        <%= link_to t(".sign_in", default: "Se connecter"), new_user_session_path, class: "navbar-item navbar-link" %>
        <%= link_to t(".sign_up", default: "Créer un compte"),  new_user_registration_path, class: "navbar-item navbar-link" %>
        </div>

    <% end %>
</div>
